define(["require"],function (require) {
    "use strict";

    function MainView() {
        this.initialized = false;
    }

    MainView.prototype.init = function() {
        if(!this.initialized) {
        }
    };

    return MainView;
});
